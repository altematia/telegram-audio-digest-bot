#!/usr/bin/env bash
set -euo pipefail

if [[ "${SSH_ORIGINAL_COMMAND:-}" =~ ^deploy\ ([0-9a-f]{40})$ ]]; then
  exec /bin/bash /opt/telegram-audio-digest-bot/scripts/deploy-server.sh "${BASH_REMATCH[1]}"
fi

echo "This SSH key is restricted to a verified application deploy." >&2
exit 1
